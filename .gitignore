# 백업 및 아카이브 (배포 안함)
backup/
backup_*/
archive/
*.backup
*.bak

# 로그 파일들 (배포 안함)
*.log
logs/
log/

# 상태 및 런타임 파일들 (서버별로 다름, 배포 안함)
proxy_state.json
config/dongle_config.json

# 서버별 자동 생성 스크립트 (init_dongle_config.sh가 생성)
scripts/restart_all_hubs.sh
scripts/usb_mapping.json

# 데이터베이스 (배포 안함)
*.db
*.sqlite
*.sqlite3
*.pid
*.lock
/tmp/

# Python 캐시 (배포 안함)
__pycache__/
*.py[cod]
*$py.class
*.so
venv/
env/

# Node.js (배포 안함)
node_modules/
npm-debug.log*
package-lock.json

# 시스템/OS 파일 (배포 안함)
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# IDE 파일들 (배포 안함)
.vscode/
.idea/
*.code-workspace
.claude/

# 민감 정보 (배포 안함)
config/secrets.conf
*.key
*.pem
*.cert

# 환경변수 파일 (배포 안함)
.env
.env.local

# 빌드 결과물 (배포 안함)
dist/
build/
*.rpm
*.deb
*.tar.gz
uhubctl/
